
# Name: roles/install_wireguard/molecule/default/molecule.yml
# Updated: 2020-10-10
#
---
dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: instance
    image: docker.io/pycontribs/centos:8
    pre_build_image: true

provisioner:
  name: ansible

verifier:
  name: ansible

lint: |
   set -e
   yamllint .
   ansible-lint

### END DBK